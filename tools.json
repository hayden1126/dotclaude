{
  "ccstatusline": {
    "installer": "bun",
    "package": "ccstatusline",
    "purpose": "Status line renderer. settings.json statusLine runs $HOME/.bun/bin/ccstatusline. Without it the status line is blank but Claude Code still works.",
    "homepage": "https://github.com/sirmalloc/ccstatusline",
    "command": "bun install -g ccstatusline"
  }
}
